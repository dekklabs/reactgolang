export const types = {
    login           : "[auth] login",
    logout          : "[auth] logout",
    logged          : "[auth] logged",

    uiStartLoading  : "[UI] startLoading",
    uiFinishLoading : "[UI] finishLoading",

    userLoad        : "[user] Load user",
    userClean       : "[user] Clean user",
    userGetAvatar   : "[user] Get Avatar",
}